# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.

name: Sync zh-CN BI terminology

on:
  push:
    branches:
      - dev-zh-cn
    paths:
      - scripts/translations/zh_bi_overrides.json
      - scripts/translations/apply_zh_bi_overrides.py
      - .github/workflows/zh-bi-translation-sync.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-translation-catalog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: dev-zh-cn

      - name: Restore fork master translation baseline
        run: |
          git fetch origin master --depth=1
          git checkout origin/master -- superset/translations/zh/LC_MESSAGES/messages.po

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install translation tooling
        run: python -m pip install --disable-pip-version-check Babel

      - name: Apply BI terminology
        run: python scripts/translations/apply_zh_bi_overrides.py --allow-missing

      # Superset master currently contains a few pre-existing zh placeholder
      # incompatibilities. Its Dockerfile intentionally compiles with `|| true`.
      # Keep the same behavior here while still surfacing the warnings in logs.
      - name: Compile zh catalog (upstream-compatible)
        run: pybabel compile --use-fuzzy -d superset/translations -l zh || true

      - name: Commit materialized catalog
        run: |
          if git diff --quiet -- superset/translations/zh/LC_MESSAGES/messages.po; then
            echo "Chinese catalog already matches BI terminology overrides."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add superset/translations/zh/LC_MESSAGES/messages.po
          git commit -m "fix(i18n): apply zh-CN BI terminology"
          git push origin HEAD:dev-zh-cn
